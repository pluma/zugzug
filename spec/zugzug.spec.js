/*global describe: false, it: false */
var expect = require('expect.js');

describe('this project\'s test-suite', function() {
  it('must be written', function() {
    expect(false).to.equal(true);
  });
});